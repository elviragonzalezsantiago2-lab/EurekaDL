Helper scripts for installing and configuring EurekaDL.

install_eureka.sh - Cross-platform installer to create a global 'eureka' wrapper

Run from the repository root or use the provided path. Example:

  bash scripts/install_eureka.sh
  bash scripts/install_eureka.sh --prefix /usr/local/bin
